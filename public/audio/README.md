# Áudio

Coloque aqui a música, por exemplo `nossa-musica.mp3`.

O caminho é configurado em `src/config/relationship.ts` (campo `music`).

Se o arquivo não existir, a aplicação não quebra — o botão de música apenas não toca nada.
